
# Environment variables for chapters-of-us
# Copy this file to .env.local and customize as needed.
# Note: .env.local is gitignored and overrides other .env files.
# Restart the dev server after changes.

# Development server port (default: 3000)
PORT=3000

# Base URL for production (e.g., for absolute image paths or links)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Optional: Override anniversary date from config.json (YYYY-MM-DD format)
NEXT_PUBLIC_ANIVERSARY_DATE=2016-10-10

# Optional: Custom image domains (add to next.config.js if more needed)
# For now, configured for placehold.co and unsplash in next.config.js
